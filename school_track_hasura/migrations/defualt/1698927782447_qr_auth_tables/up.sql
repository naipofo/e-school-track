CREATE TABLE "public"."qr_auth" ("hash" text NOT NULL, "user_id" integer NOT NULL, PRIMARY KEY ("hash") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE restrict ON DELETE restrict);
